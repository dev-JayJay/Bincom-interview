import { Model, RelationMappings, RelationMappingsThunk } from "objection";


class PollingUnit extends Model {
    static tableName = "polling_unit";

    static relationMappings = {
        
    }
}

export default PollingUnit;